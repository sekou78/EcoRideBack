<form method="post" action="{{ path('app_profil_conducteur_delete', {'id': profil_conducteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil_conducteur.id) }}">
    <button class="btn">Delete</button>
</form>
